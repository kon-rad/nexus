/**
 * Nexus orchestrator — Phase 2 stub.
 *
 * This file is a placeholder. Phase 2 fills in:
 *   - cursor.ts          (Cursor SDK agent loop)
 *   - daytona.ts         (sandbox lifecycle)
 *   - gemini-router.ts   (intent → start_build)
 *   - convex-pusher.ts   (event fan-out to Convex)
 *
 * For now, the orchestrator just compiles cleanly so the workspace builds end-to-end.
 */

export const ORCHESTRATOR_VERSION = "0.0.1";

if (require.main === module) {
  // eslint-disable-next-line no-console
  console.log(`[nexus-orchestrator ${ORCHESTRATOR_VERSION}] phase 2 stub — not yet implemented`);
}
