export default function LandingPage() {
  // Replaced in task 1.5
  return (
    <main style={{ padding: 32, color: "var(--text-primary)" }}>
      <h1 style={{ fontSize: 24, fontWeight: 600 }}>Nexus</h1>
      <p style={{ color: "var(--text-secondary)" }}>
        Landing page placeholder — built out in task 1.5.
      </p>
    </main>
  );
}
